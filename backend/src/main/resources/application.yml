spring:
  application:
    name: backend
  r2dbc:
    url: r2dbc:mysql://rm-cn-fsw46k36h000b7o.rwlb.rds.aliyuncs.com:3306/software_implementation
    username: broky
    password: Scnu2022!
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect

server:
  port: 8080
springdoc:
  swagger-ui:
    path: /swagger-ui.html
    tags-sorter: alpha
    operations-sorter: alpha
  api-docs:
    path: /v3/api-docs
  group-configs:
    - group: 'default'
      paths-to-match: '/**'
      packages-to-scan: com.xiaominfo.knife4j.demo.web

jwt:
  secret: "your-256-bit-secret-key-must-be-at-least-32-characters-long"
  access-token:
    expiration: 3600000
  refresh-token:
    expiration: 2592000000